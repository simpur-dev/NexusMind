# 计算机设计大赛人工智能实践赛功能演示视频脚本

## 一、演示目标

本视频用于展示项目如何围绕一个真实案例，完成从材料接入、事实基线构建、图谱分析、预测推演到决策输出的完整闭环。

核心展示思路不是逐页讲按钮，而是围绕真实事件的演化过程，体现系统的三项关键能力：

- **动态演化**：不是一次性静态分析，而是随着事件发展持续吸收新材料、更新认知。
- **多基线版本**：系统会保留事件在不同时间点的事实基线，支持版本切换与差异对比。
- **决策闭环**：从材料理解、图谱构建、预测分支到决策简报，形成面向真实问题的完整链路。

建议整条视频始终围绕一句主线展开：

> 我们不是只对事件做一次分析，而是随着事件不断演化，持续更新事实基线、图谱理解和决策判断。

---

## 二、推荐演示结构

建议采用“**登录后首页启动 -> 五步闭环 -> 控制台持续演化**”的结构：

### 第一部分：登录后从 `准备就绪` 页发起第一轮模拟

登录后不要先进入事件工作台，而是直接停留在首页的 `准备就绪` 页面，用第一阶段材料完成：

- `文件上传` 或 `网络抓取`
- 输入 `02 / 模拟提示词`
- 点击 `启动引擎`
- 进入 `Process` 页跑完 5 步主链路

这一部分的目标是回答：

- **系统能不能从早期有限信息直接起跑？**
- **系统能不能完整跑通 5 步闭环？**

### 第二部分：五步跑完后转入事件控制台，展示事件继续演化

当主链路跑完后，再用一句转折说明：真实事件不会停在第一次推演之后，新的种子材料会不断出现。然后进入事件控制台，逐步追加第二阶段、第三阶段、第四阶段材料，展示系统如何：

- 继续接入新材料
- 生成新基线版本
- 切换不同时间点的认知快照
- 看到图谱和风险理解同步变化
- 校准预测分支
- 更新决策简报

这一部分的目标是回答：

- **系统能不能跟踪真实事件的持续演化过程？**
- **系统是不是具备滚动更新和持续决策支持能力？**

---

## 三、推荐视频总时长

建议控制在 **5 分钟左右**。

推荐时间分配如下：

- **0:00 - 0:20**：登录后首页定位与五步流程展示
- **0:20 - 0:56**：在 `准备就绪` 页提交第一阶段材料并启动引擎
- **0:56 - 1:58**：跑完整个主链路 5 步
- **1:58 - 2:12**：转折：真实事件继续发生，种子材料增加
- **2:12 - 3:00**：进入事件控制台，追加后续阶段材料
- **3:00 - 3:48**：基线演化与图谱变化
- **3:48 - 4:20**：预测分支校准与重新推演
- **4:20 - 5:05**：决策简报与最终收尾

---

## 四、录制前准备

正式录制前建议提前准备以下内容：

### 1. 案例材料准备

- 将一个真实案例拆分为 **阶段1 / 阶段2 / 阶段3 / 阶段4**
- 阶段1材料用于在首页 `准备就绪` 页直接发起第一次模拟
- 阶段2至阶段4材料用于后半段进入事件控制台后的滚动追加
- 确保阶段划分能够体现事件认知的变化

### 2. 系统状态准备

建议提前准备好以下状态，避免录制时长时间等待：

- 已登录并默认停留在首页 `准备就绪` 页面
- 阶段1材料保存在本地固定文件夹，方便录制时选择上传
- `模拟提示词` 已提前整理好，可直接粘贴
- 点击 `启动引擎` 后能顺利进入 `Process` 页
- 跑完五步后能正常进入 `事件工作台`
- 至少一个演化后的基线、预测分支和决策简报已能稳定展示结果

### 3. 录屏策略

- 不需要录登录输入过程，建议直接从已登录首页开始
- 保留点击和操作动作
- 构建、推演、生成结果的等待过程可以提前跑好，录制时通过剪辑衔接
- 全程只使用同一个案例，不要频繁切换案例

---

## 五、完整演示逻辑

## 1. 开场：项目定位与案例引入

### 画面建议

- 登录后直接进入首页 `准备就绪` 页
- 停留在 `准备就绪` 标题、左侧 `工作流序列`、右侧 `文件上传 / 网络抓取`、底部 `启动引擎`
- 不要录登录输入过程，直接从已登录状态开始

### 旁白建议

大家好，我们展示的是一套面向高校真实事件动态研判的智能决策系统。
登录后，系统会直接进入启动页。我们可以从这里提交第一阶段种子材料，直接发起第一次模拟，并完整跑通后续五步流程。

在这个案例中，我们将真实事件拆分为多个阶段。接下来，我们先基于第一阶段已知材料完成第一次闭环，再随着后续材料的加入，展示系统如何持续更新事实理解和决策建议。

### 这一段的核心目的

- **先讲清系统解决什么问题**
- **让评委明白系统不是从“已有项目”开始，而是可以从现实种子材料直接起跑**

---

## 2. 第一阶段：先跑通完整主链路

### 画面建议

- 在首页 `文件上传` 提交阶段1材料
- 在 `02 / 模拟提示词` 中输入本次案例的决策问题
- 点击 `启动引擎`
- 进入 `Process` 页，快速讲完 5 步完整流程

### 建议讲法

这里我们首先只使用第一阶段的公开材料，在首页直接提交现实种子并启动引擎。
系统会先将原始材料纳入统一上下文，形成初始事实基线；随后从非结构化文本中抽取实体、关系和事件线索，构建事件因果图谱；在此基础上进一步建立推演环境，生成预测分支，并输出结构化的决策结果。

通过这一轮演示，评委可以先看到：这不是单点功能，而是一条从现实种子接入到预测分析再到决策输出的完整闭环。

### 本段建议强调的五个概念

- **现实种子接入**
- **事实基线**
- **图谱构建**
- **预测推演**
- **结果输出**

### 注意事项

- 这一段要快，不要逐个按钮细讲
- 重点是让评委先明白系统可以从首页直接发起完整流程

---

## 3. 转折：进入事件控制台，展示后续阶段材料的追加

### 画面建议

- 在 `Process` 页跑完五步后，点击顶部 `事件工作台`
- 进入事件控制台
- 打开“追加材料”功能
- 演示文件上传或网络抓取
- 逐步加入阶段2、阶段3、阶段4材料

### 旁白建议

到这里，第一轮闭环已经跑完。但真实事件不会停在第一次推演之后，新的种子材料会不断出现。
现在我们进入事件控制台，把第二阶段、第三阶段以及后续阶段材料继续追加进来。

这里既支持本地文件上传，也支持基于关键词的网络抓取，可以把多源信息统一纳入同一个事件上下文中。

当新增材料进入后，系统不会简单覆盖旧结论，而是生成新的事实基线版本，从而保留事件在不同时间点的认知状态。

### 本段要体现的价值

- **事件控制台不是普通材料页，而是动态更新的操作中枢**
- **系统支持多源材料汇聚**
- **系统支持面向时间演化的基线版本管理**

---

## 4. 基线演化与图谱变化

### 画面建议

- 展示不同基线版本，例如 v1、v2、v3
- 切换基线版本
- 展示图谱或图谱统计同步变化
- 说明不同版本对应不同时间点的认知快照

### 旁白建议

随着事件进入新的阶段，系统会基于新增材料形成新的事实基线。
在这里我们可以看到，不同版本分别对应事件在不同时间点的事实快照。
切换不同基线后，图谱结构、风险判断和后续分析结果也会发生同步变化。

这说明系统不是对事件做一次静态分析，而是在事件演化过程中不断修正认知和判断。

### 本段必须强调的亮点

- **多基线版本管理**
- **可追踪事件认知如何随着信息更新而变化**
- **图谱与基线联动，而不是固定不变**

---

## 5. 预测分支与校准

### 画面建议

- 展示当前基线下的预测分支列表
- 说明不同分支代表不同未来路径或干预方案
- 展示启动、查看、校准或重新预测相关过程

### 旁白建议

在同一个事实基线之上，系统支持建立多个预测分支，用于模拟不同策略或不同发展路径下事件可能出现的变化。

当新的阶段材料进入后，我们还可以基于新的基线对预测分支进行校准和重新推演，避免后续判断依赖过时信息。

因此，系统不仅能回答“当前发生了什么”，还能进一步回答“接下来可能如何发展”以及“不同干预策略会带来什么影响”。

### 本段建议强调

- **预测分支体现未来路径推演能力**
- **校准能力体现系统能够适应事件演化**
- **这是人工智能实践赛中非常有竞争力的展示点**

---

## 6. 决策简报与最终收尾

### 画面建议

- 点击“获取决策简报”
- 展示推荐动作、关键风险、证据、监测信号等内容
- 最后如有报告页或结果页，可进行收束展示

### 旁白建议

最终，系统会结合当前事实基线与当前预测分支，生成结构化的决策简报。
这意味着决策简报并不是一个固定模板，而是由事件在当前阶段的事实状态和当前推演结果共同决定。

因此，随着新材料进入、基线更新和分支变化，决策简报内容也会动态调整。

通过这个案例，我们展示了系统如何从材料接入、事实演化、图谱构建、预测推演一直到决策输出，形成完整的智能决策闭环。

谢谢大家。

### 收尾时建议强调的三句话

- **不是静态分析，而是动态演化**
- **不是单次结论，而是多基线版本管理**
- **不是单点工具，而是从认知到推演再到决策的完整闭环**

---

## 六、页面讲解时的表达重点

为了避免视频变成“界面说明书”，每一页建议都围绕“它解决什么问题”来讲，而不是围绕“这里有什么按钮”来讲。

### 1. 登录后启动页（`准备就绪` 页）

建议强调：

- **这不是普通首页，而是从现实种子材料直接发起模拟的入口**
- **左侧五步工作流让评委一眼看到完整链路**
- **右侧输入区说明系统可以从早期有限信息直接起跑**

### 2. 事件控制台

建议强调：

- **这是事件全生命周期的总控台**
- **材料汇聚、基线演化、预测分支管理都在这里完成**
- **适合动态跟踪真实事件**

### 3. 主链路页面（`Process` 页）

建议强调：

- **把非结构化材料转为结构化认知**
- **完成图谱、环境、模拟和输出之间的闭环**

### 4. 基线版本区域

建议强调：

- **系统不是只保留当前结果，而是保留不同时间点的事实快照**
- **评估者可以追踪认知随事件演化的变化过程**

### 5. 预测分支区域

建议强调：

- **面向未来路径与干预方案的对比推演**
- **支持在新基线下重新校准和重跑**

### 6. 决策简报区域

建议强调：

- **不是普通摘要，而是基线和预测分支共同作用下的结构化建议**
- **直接服务于研判和决策**

---

## 七、可直接使用的精简口播稿

大家好，我们展示的是一套面向真实事件动态研判的智能决策系统。
登录后，系统会直接进入启动页。这里既展示完整的五步工作流，也支持我们直接提交第一阶段种子材料，发起第一次模拟。

在这个案例中，我们将真实事件拆分为多个阶段。首先，我们只使用第一阶段材料，从首页直接启动系统。
系统会先汇聚种子材料，形成初始事实基线；随后从非结构化文本中抽取实体与关系，构建事件因果图谱；在此基础上建立推演环境，生成预测结果，并输出结构化结果。

当第一次闭环跑完后，我们再进入事件控制台。随着事件进入后续阶段，这里可以持续追加新材料。既支持文件上传，也支持网络抓取，能够把多源信息持续纳入同一个事件上下文。
每次新材料进入后，系统都会生成新的基线版本，从而保留事件在不同时间点的认知状态。切换不同基线，我们可以看到图谱结构、风险判断和后续推演都会发生变化。

在新的基线之上，系统还支持对预测分支进行校准和重新推演，用来模拟不同策略下事件的发展路径。
最后，系统结合当前基线和当前预测分支生成决策简报，给出风险、证据、监测信号和推荐动作。

通过这个案例，我们展示了系统如何从早期有限信息起跑，再到后续滚动补充材料，形成一个完整的动态智能决策闭环。

---

## 八、录制注意事项

### 1. 避免长时间等待

- 构建图谱、推演运行、报告生成等过程尽量提前准备
- 录制时展示点击动作，再用剪辑连接到结果状态

### 2. 全程围绕同一个案例

- 不要中途切换案例
- 保证叙事连贯，让评委形成完整印象

### 3. 每一段只讲一个核心价值

例如：

- 启动页讲“现实种子接入 + 一键起跑”
- 控制台讲“动态更新”
- 基线讲“版本演化”
- 分支讲“未来推演”
- 简报讲“决策支持”

### 4. 多讲“为什么”，少讲“怎么点”

评委更关心：

- 你们解决了什么问题
- 为什么这样设计
- 这个系统和普通数据展示工具有什么区别

---

## 九、最终建议

整条视频建议始终围绕以下主线推进：

> 先在登录后首页用第一阶段材料直接启动系统并跑通五步，再用后续阶段证明系统能随着真实事件演化持续更新认知、推演结果和决策建议。

如果时间允许，可以在视频字幕中加入以下章节标题：

- **1. 现实种子接入**
- **2. 五步主链路启动**
- **3. 事件控制台滚动更新**
- **4. 基线版本演化**
- **5. 预测分支校准**
- **6. 决策简报输出**

这样能让视频整体更像比赛作品，而不是普通录屏。
