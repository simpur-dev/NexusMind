# 🐟 MiroFish 系统架构设计文档 (详细版)

## 1. 项目概述
**MiroFish** 是一款基于大语言模型（LLM）与多智能体（Multi-Agent）技术的新一代群体智能预测引擎。
项目的核心定位是**数字平行世界的沙盘推演系统**。通过解析现实世界的种子信息（如新闻事件、政策草案、文学剧本等），系统能够自动构建出高保真的知识图谱与社会环境，并在其中投放大量具备独立人格、记忆和行为逻辑的智能体（Agents）。
- **适用场景**：公共事件舆情推演、金融市场情绪预测、企业公关危机预演、文学作品开放式结局推演等。
- **核心业务目标**：通过捕获个体级交互引发的群体“涌现”行为，将传统的静态预测转化为动态、可视化的数字沙盘演练，帮助用户在“零风险”的虚拟环境中观察决策的长期连锁反应。

## 2. 核心价值与问题解决
- **核心业务痛点解决**：
  - **决策试错成本高**：现实世界中，一项新政策或公关声明发布后无法撤回。本项目通过沙盘预演，让决策者在事前就能看到可能的社会反响。
  - **传统预测缺乏动态博弈**：传统数据分析往往基于静态历史数据，无法模拟真实世界中人与人之间复杂的情绪感染与观点交锋。
- **带来的核心价值**：
  - **宏观战略价值**：为企业或政府提供“如果...会怎样”的低成本试错实验室。
  - **微观创新价值**：为普通用户提供创意探索工具（如小说推演），极大降低了多智能体社会模拟的技术门槛。

## 3. 整体架构设计
项目采用**经典的前后端分离（B/S）单体架构**，核心逻辑后置，以异步任务驱动重度计算。前后端通过 RESTful API 进行通讯，内部重度依赖第三方 SaaS API 进行大模型推理和知识检索。

**【顶层架构视图】**
```text
┌─────────────────────────────────────────────────────────────────────────┐
│                          表现层 (Frontend)                               │
│  框架: Vue 3 + Vite + Vue Router                                          │
│  组件: Step1~Step5 业务视图, GraphPanel (D3.js 图谱渲染)                  │
└────────────────────────────────────┬────────────────────────────────────┘
                                     │ (RESTful API, axios)
┌────────────────────────────────────▼────────────────────────────────────┐
│                        网关与路由层 (Backend API)                          │
│  框架: Flask 3.x + Flask-CORS                                            │
│  路由: /api/graph (图谱), /api/simulation (模拟), /api/report (报告)        │
└────────────────────────────────────┬────────────────────────────────────┘
                                     │ (Controller -> Services)
┌────────────────────────────────────▼────────────────────────────────────┐
│                        核心业务逻辑层 (Services)                           │
│  ├─ 知识构建: GraphBuilderService, OntologyGenerator, TextProcessor       │
│  ├─ 环境生成: OasisProfileGenerator, SimulationConfigGenerator           │
│  ├─ 仿真引擎: SimulationManager, SimulationRunner (进程隔离)               │
│  └─ 智能报告: ReportAgent (LangChain ReACT), zep_tools (工具集)            │
└────────────────────────────────────┬────────────────────────────────────┘
                                     │ (SDKs, HTTP Requests, IPC)
┌────────────────────────────────────▼────────────────────────────────────┐
│                        基础设施与能力层 (Infrastructure)                    │
│  ├─ Zep Cloud: 承载 GraphRAG 抽取、图谱存储与 Agent 长期时序记忆管理        │
│  ├─ CAMEL-AI OASIS: 驱动 Twitter/Reddit 多平台高拟真社交网络交互模拟       │
│  ├─ LLM Provider: 兼容 OpenAI 格式的大模型 API (如 阿里云 Qwen-Plus)      │
│  └─ 状态持久化: TaskManager / ProjectManager (基于本地 JSON 与内存锁)       │
└─────────────────────────────────────────────────────────────────────────┘
```

## 4. 核心模块拆解
依据源码 `backend/app/services/` 和 `backend/app/api/` 目录结构，系统核心拆解为四大子模块：

### 4.1 图谱构建模块
- **核心文件**：`graph_builder.py`, `ontology_generator.py`, `text_processor.py`, `api/graph.py`
- **职责**：负责将用户上传的 TXT/PDF 解析分块，调用 Zep API 抽取实体、关系并构建出 Standalone Graph。
- **关键逻辑**：
  - **文档解析**：使用 `PyMuPDF` 和 `charset-normalizer` 解决多格式文本与编码问题。
  - **异步构建**：大文件图谱构建极耗时，`GraphBuilderService` 内部使用 `threading.Thread` 在后台运行构建任务。
  - **状态追踪**：通过单例 `TaskManager` 在内存中记录进度 (`progress`, `message`)，前端通过轮询 `GET /api/graph/build/status` 获取实时状态。

### 4.2 仿真环境配置模块
- **核心文件**：`oasis_profile_generator.py`, `simulation_config_generator.py`, `zep_entity_reader.py`
- **职责**：读取知识图谱中的核心实体，利用 LLM 将其转化为具体的 OASIS Agent 角色（包括背景、性格、动机）。
- **关键逻辑**：
  - **实体过滤**：`ZepEntityReader` 过滤掉无意义实体，提取核心人物和组织。
  - **角色注入**：`OasisProfileGenerator` 使用 Prompt 模板要求大模型为每个实体生成符合 `OasisAgentProfile` 数据类规范的角色卡片。
  - **环境生成**：`SimulationConfigGenerator` 根据用户的预测目标，自动生成社交平台（Twitter/Reddit）的初始贴文与话题趋势。

### 4.3 并行模拟调度模块
- **核心文件**：`simulation_manager.py`, `simulation_runner.py`, `scripts/run_parallel_simulation.py`
- **职责**：整个引擎的心脏。负责拉起并管理基于 CAMEL-OASIS 的模拟子进程。
- **关键逻辑**：
  - **进程隔离**：`SimulationRunner` 使用 `subprocess.Popen` 拉起独立的 Python 脚本 (`run_parallel_simulation.py`) 执行模拟，防止模拟崩溃导致 Flask 主进程挂掉。
  - **状态持久化**：采用极简的文件系统持久化。`SimulationManager` 将状态保存为 `backend/uploads/simulations/{sim_id}/state.json`。
  - **生命周期控制**：支持通过 API 实现模拟的启动、停止（清理子进程）。

### 4.4 报告生成与智能体交互模块
- **核心文件**：`report_agent.py`, `zep_tools.py`, `api/report.py`
- **职责**：模拟结束后，自动生成详尽的预测报告；支持用户与特定的沙盘 Agent 对话。
- **关键逻辑**：
  - **ReACT 架构**：`ReportAgent` 采用了基于 LangChain 思想的 ReACT 模式（Reasoning and Acting）。通过规划（Planning）-> 行动（Action/Tool）-> 反思（Reflection）多轮思考来生成报告。
  - **外挂工具集**：`ZepToolsService` 提供了一系列 Agent 可调用的工具，如 `search_memory`（检索交互历史）、`analyze_trends`（分析全景趋势）。
  - **日志链路**：内置 `ReportLogger`，将 Agent 的每一次思考、工具调用详细记录为 `agent_log.jsonl`，解决 LLM 调试黑盒问题。

## 5. 技术栈选型说明

### 5.1 前端技术栈
- **核心框架**：`Vue 3` (Composition API) + `Vite` 构建工具。
- **路由管理**：`Vue Router 4` 实现单页应用（SPA）的步骤流转。
- **可视化引擎**：`D3.js (^7.9.0)`，用于高定制化的图谱节点与关系拓扑图渲染 (`GraphPanel.vue`)。
- **网络请求**：`Axios` 封装的 REST API 客户端 (`api/index.js`)。

### 5.2 后端技术栈
- **核心语言与包管理**：`Python 3.11/3.12`，使用 `uv` / `pip` 进行极速依赖管理 (`pyproject.toml`)。
- **Web 框架**：`Flask >= 3.0.0`，采用 Blueprint 模式拆分模块路由，搭配 `Flask-CORS` 解决跨域。
- **文档处理**：`PyMuPDF` (PDF抽取), `charset-normalizer` (编码检测)。

### 5.3 核心 AI 与依赖组件
- **知识图谱与记忆外脑**：`Zep Cloud (zep-cloud==3.13.0)`。核心知识库。承担了复杂的 GraphRAG 抽取与海量 Agent 记忆的时序管理，大幅减轻了本地数据库的压力。
- **多智能体模拟引擎**：`CAMEL-OASIS (camel-oasis==0.2.5)`。直接提供高度拟真的 Twitter/Reddit 信息流交互环境的底层库。
- **大模型通信**：`OpenAI SDK (openai>=1.0.0)`。作为标准化大模型接入层，可通过环境变量灵活切换任何兼容 OpenAI 接口的模型（官方推荐使用阿里云 `qwen-plus`）。

## 6. 核心流程与数据流
业务核心链路分为 5 个标准步骤（对应前端视图的 Step 1 - Step 5）：

1. **输入与解析（Step 1）**：
   - 用户上传 PDF/TXT -> `POST /api/graph/ontology/generate` -> `FileParser` 提取文本 -> `OntologyGenerator` 生成本体。
   - 用户确认本体 -> `POST /api/graph/build` -> `GraphBuilderService` 后台切块推入 Zep Cloud 构建 GraphRAG -> 返回任务进度。
2. **环境搭建（Step 2）**：
   - 触发环境生成 -> `POST /api/simulation/prepare` -> 从 Zep 读取图谱实体 -> `OasisProfileGenerator` 调用 LLM 生成 Agent 人设文件 -> `SimulationManager` 生成 `state.json`。
3. **推演执行（Step 3）**：
   - 启动推演 -> `POST /api/simulation/run` -> `SimulationRunner` 拉起 `subprocess` 运行 `run_parallel_simulation.py`。
   - CAMEL-OASIS 引擎运转，Agent 产生社交动作（发帖、点赞） -> 互动记录通过 `zep_graph_memory_updater.py` 刷入 Zep 记忆库。
4. **结果提炼（Step 4）**：
   - 推演结束 -> `POST /api/report/generate` -> 触发 `ReportAgent` 后台任务。
   - `ReportAgent` 循环调用 `zep_tools.py`（规划大纲 -> 检索数据 -> 思考与反思 -> 生成 Markdown） -> 最终报告写入 `uploads/reports/{id}/report.md`。
5. **深度交互（Step 5）**：
   - 交互视图 -> `POST /api/simulation/chat` -> 选取特定的沙盘 Agent -> 后端拉取该 Agent 在 Zep 中的最新上下文，结合当前世界观回答用户追问。

## 7. 接口与依赖设计

### 7.1 核心对外 API 设计
所有 API 以 `/api` 区分命名空间，统一使用 JSON 格式响应 `{ "success": bool, "data": dict, "error": string }`：
- **图谱域 (`/api/graph/*`)**：
  - `POST /ontology/generate`: 接收 `multipart/form-data` 文件上传，返回抽取出的图谱本体。
  - `POST /build`: 传入本体定义和文本，启动异步图谱构建任务，返回 `task_id`。
  - `GET /build/status/<task_id>`: 轮询查询构建进度。
- **模拟域 (`/api/simulation/*`)**：
  - `POST /prepare`: 基于图谱实体和要求，准备 OASIS 的配置文件。
  - `POST /run`: 启动物理级模拟进程。
  - `GET /status/<sim_id>`: 获取当前推演进行到了第几轮，以及各个平台的健康度。
  - `POST /stop`: 强杀模拟进程。
- **报告域 (`/api/report/*`)**：
  - `POST /generate`: 启动报告生成任务（同样返回 `task_id`）。
  - `GET /content/<report_id>`: 获取已生成的 Markdown 报告内容。

### 7.2 内部状态与文件依赖
系统避免了引入传统关系型数据库（如 MySQL），通过极简的本地文件与内存单例管理状态：
- **`models/project.py`**: 使用内存字典存储 `Project` 对象。
- **`models/task.py`**: `TaskManager` 使用线程锁 (`threading.Lock()`) 管理所有耗时任务（`Task`）的状态更新。
- **文件存储结构**：
  - `backend/uploads/`：存放上传的原始文本。
  - `backend/uploads/simulations/{sim_id}/`：存放模拟的环境配置（`profiles.json`）、状态快照（`state.json`）。
  - `backend/uploads/reports/{report_id}/`：存放生成的报告 (`report.md`) 和日志 (`agent_log.jsonl`)。

## 8. 部署与运行架构
- **部署模式**：支持原生源码部署（Node + Python 虚拟环境）与 Docker 容器化部署。
- **运行环境**：
  - 基于 `docker-compose.yml` 实现单机一键编排。
  - 容器内暴露 `3000` 端口用于访问前端，`5001` 端口用于后端 API 通信。
  - **环境变量依赖**：系统强依赖根目录下的 `.env` 文件（必需项：`LLM_API_KEY`, `LLM_BASE_URL`, `ZEP_API_KEY`）。
  - **持久化设计**：通过 Docker Volume 映射 `./backend/uploads:/app/backend/uploads`，确保容器重启后模拟状态文件不丢失。

## 9. 可维护性与扩展性说明
- **架构扩展性（Extensibility）**：
  - **LLM 客户端解耦**：封装了统一的 `LLMClient` 工具类 (`backend/app/utils/llm_client.py`)，带有 `retry` 装饰器抗网络抖动，未来更换底层大模型供应商无需修改核心业务逻辑。
  - **工具链解耦**：ReACT Agent 使用的工具集被统一封装在 `zep_tools.py`，新增一种数据分析视角（如“情绪曲线分析”）仅需新增一个 Python 方法并注册。
- **可维护性设计（Maintainability）**：
  - **完备的日志与调试链路**：后端的 `logger.py` 区分了业务日志与请求日志。此外，`ReportLogger` 将 Agent 的 ReACT 推理过程全量落盘，极大降低了复杂多轮 LLM 交互导致的“幻觉”定位难度。

## 10. 架构风险与优化建议
基于源码的深度逆向解析，当前架构在支撑企业级高可用时存在以下潜在技术债务，需在未来迭代中优化：

1. **单机状态强耦合导致无法水平扩展**：
   - **风险**：当前的 `TaskManager` 和 `ProjectManager` 是基于 Python 进程内全局变量实现的（见 `app/models/task.py`）。如果使用 Gunicorn 或多容器实例部署（负载均衡），将面临极其严重的状态割裂与找不到任务 ID 的问题。同时，进程重启将导致所有历史项目数据清空（目前仅有 `simulations/` 目录落盘）。
   - **建议**：引入 **Redis** 替代内存字典进行状态缓存和进度追踪；引入 **PostgreSQL/MongoDB** 替代内存进行项目元数据的持久化存储，将应用层改造为无状态（Stateless）架构。
2. **异步任务调度与 GIL 阻塞风险**：
   - **风险**：当前系统的耗时任务（如构建图谱 `GraphBuilderService._build_graph_worker`）是直接通过 `threading.Thread` 在 Web 主进程内启动的。在 Python 的全局解释器锁（GIL）机制下，高并发请求极易导致 Flask 响应阻塞；且若主进程崩溃，所有进行中的线程任务将静默死亡。
   - **建议**：剥离重度后台任务，引入专业的异步任务队列架构（如 **Celery + RabbitMQ/Redis**）。将图谱上传、报告生成、OASIS 模拟运行等任务交由独立的 Worker 节点执行。
3. **外部依赖 SaaS 的成本与网络黑洞**：
   - **风险**：核心引擎强绑定了 Zep Cloud 的公有云 API。这不仅使得数据出境面临合规和隐私风险（在分析私有财报或敏感舆情时尤为明显），而且海量 Agent 在模拟中产生的高频记忆交互会产生极其高昂的 API 调用成本。
   - **建议**：在架构层（`backend/app/services/`）设计一层抽象的 `Memory/Graph Adapter` 接口。短期内支持接入本地化部署的开源平替方案（如使用 **Milvus 向量库 + Neo4j 图数据库 + LlamaIndex** 组合实现本地 GraphRAG），以降低私有化交付门槛和长期运营成本。