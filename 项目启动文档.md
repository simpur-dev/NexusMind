# NexusMind 项目环境配置与启动指南

本文档记录了 `NexusMind` 项目的本地环境配置和一键启动的完整流程。

## 1. 环境准备

在开始之前，请确保您的本地环境已经安装了以下工具：
- **Node.js** (版本 >= 18.0.0)
- **Python** (版本 >= 3.11)
- **Git**

## 2. 获取代码

将项目代码克隆到本地目录：
```bash
git clone https://github.com/666ghj/NexusMind
cd NexusMind
```

## 3. 安装依赖

该项目是一个前后端分离架构，分别需要安装 Node.js 和 Python 的依赖包。

### 3.1 安装前端依赖

前端代码位于 `frontend` 目录。
```bash
# 在项目根目录执行
npm install

# 进入前端目录并安装依赖
cd frontend
npm install
```

### 3.2 安装后端依赖

后端代码位于 `backend` 目录，推荐使用虚拟环境来隔离 Python 依赖。
```bash
# 从项目根目录进入后端目录
cd backend

# 创建 Python 虚拟环境 (以 Windows PowerShell 为例)
python -m venv venv
.\venv\Scripts\activate

# 安装 Python 依赖包
pip install -r requirements.txt
```

## 4. 环境变量配置

项目依赖大型语言模型（LLM）和 Zep 记忆图谱，必须配置相应的 API Key 才能正常运行。

1. 在 `backend` 目录下，找到 `.env.example` 文件。
2. 将其复制并重命名为 `.env`。
3. 填入您的 API Key。

示例 `.env` 配置（以阿里云百炼平台 `qwen-plus` 模型为例）：
```env
# ===== LLM API 配置 =====
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_NAME=qwen-plus

# ===== ZEP 记忆图谱配置 =====
# 在 https://app.getzep.com/ 免费获取
ZEP_API_KEY=z_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

## 5. 启动项目

您可以分别启动前端和后端服务：

### 5.1 启动后端服务
确保您已经在 `backend` 目录下并激活了虚拟环境：
```bash
# 确保在 backend 目录下且虚拟环境已激活
python run.py
```
后端服务默认将在 `http://localhost:5001` 启动。

### 5.2 启动前端服务
新开一个终端，进入 `frontend` 目录：
```bash
cd frontend
npm run dev
```
前端服务默认将在 `http://localhost:3000` 启动。

### 5.3 一键启动 (可选)
如果您在项目根目录运行了 `npm install` 安装了 `concurrently`，您也可以尝试在根目录使用一键启动脚本（前提是后端配置了全局环境或者您使用 `uv` 包管理器）：
```bash
npm run dev
```

## 6. 访问应用

当前后端服务都成功启动后，打开浏览器访问：
**http://localhost:3000/**

您现在可以开始体验 NexusMind 强大的预测与模拟功能了！
